<Page x:Class="eTOM.maingui"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:eTOM"
      
      xmlns:converters="clr-namespace:eTOM"
      
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="maingui">

    <Page.Resources>
        <Style  TargetType="{x:Type TabItem}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Margin" Value="-2,-2,0,2"></Setter>
            <Setter Property="Foreground" Value="#ffb56b"/>
            <Setter Property="Background" Value="#CC7218"/>
            <Setter Property="BorderBrush" Value="#CC7218"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="MaxHeight" Value="80"/>
            <Setter Property="MaxWidth" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"  >
                            <Grid >
                                <ContentPresenter HorizontalAlignment="Center" Margin="12,2,12,2" x:Name="ContentSite" VerticalAlignment="Center" RecognizesAccessKey="True" ContentSource="Header" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#CC7218"/>
                                <Setter Property="Background" Value="#ffb56b"/>
                                <Setter Property="BorderBrush" Value="#ffb56b"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>


                </Setter.Value>
            </Setter>

        </Style>


        <local:SampleData x:Key="SampleData"/>
        <converters:ListToTextEncodingConverter x:Key="ListToTextEncodingConverter"/>
    </Page.Resources>

    <Grid Background="#CC7218">


        <TabControl DockPanel.Dock="Top" Background="#ffb56b" BorderBrush="#ffb56b" TabStripPlacement="Left"  x:Name="TabControl">

            <TabItem x:Name="tabItem" Header="Клиенты">
                <TabItem.Content>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="60"></RowDefinition>
                            <RowDefinition Height="295"></RowDefinition>
                            <RowDefinition Height="0*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <DataGrid Background="#ffb56b" AutoGenerateColumns="False"  Grid.Row="1" x:Name="clients" ItemsSource="{Binding Path=Clients}" CanUserResizeRows="False" >
                            <DataGrid.Resources>
                                <Style TargetType="TextBlock" x:Key="WrapText">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Visibility="Hidden" Header="id" Width="*" Binding="{Binding Path=id}" ElementStyle="{StaticResource WrapText}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Имя" Width="*" Binding="{Binding Path=name_client}" ElementStyle="{StaticResource WrapText}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Фамилия" Width="*" Binding="{Binding Path=surname}" ElementStyle="{StaticResource WrapText}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Отчество" Width="*" Binding="{Binding Path=fathername}" ElementStyle="{StaticResource WrapText}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Номер договора" Width="*" Binding="{Binding Path=contractnumb}" ElementStyle="{StaticResource WrapText}" ></DataGridTextColumn>
                                <DataGridTextColumn Header="Адрес" Width="*" Binding="{Binding Path=address}" ElementStyle="{StaticResource WrapText}" ></DataGridTextColumn>

                                <DataGridTemplateColumn Header="Действие" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="Client_edit_click">Подробнее</Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>

                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Canvas>
                            <Button Content="Перезагрузить" Canvas.Left="290" Canvas.Top="364" HorizontalAlignment="Left" Height="40" VerticalAlignment="Center" Click="Reload_page"  Width="92"></Button>
                            <Button Content="Добавить" Canvas.Left="22" Canvas.Top="364" HorizontalAlignment="Left" Height="40" VerticalAlignment="Center" Width="92" Click="ClientAdd_click"/>
                            <Button x:Name="otchet" Content="Создать отчет" Canvas.Left="580" Canvas.Top="364" Height="40" Click="Client_excel"  Width="92" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <ComboBox Name="searchParam" IsEditable="False" IsReadOnly="True" Canvas.Left="241" Canvas.Top="19" HorizontalAlignment="Left" VerticalAlignment="Center" Width="141">
                                <ComboBoxItem Content="Имя"></ComboBoxItem>
                                <ComboBoxItem Content="Фамилия"></ComboBoxItem>
                                <ComboBoxItem Content="Отчество"></ComboBoxItem>
                                <ComboBoxItem Content="Номер договора"></ComboBoxItem>
                                <ComboBoxItem Content="Адрес"></ComboBoxItem>
                            </ComboBox>
                            <TextBox x:Name="searchText" Canvas.Left="397" Canvas.Top="21" HorizontalAlignment="Left" VerticalAlignment="Center" Width="140"></TextBox>
                            <Button Content="Поиск" Click="findClient" Canvas.Left="554" Canvas.Top="20" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Width="65"></Button>
                        </Canvas>
                    </Grid>
                </TabItem.Content>

            </TabItem>
            <TabItem Header="Заявки">
                <TabItem.Content >
                    <Grid Height="445" Width="695">
                        <TabControl x:Name="Zayavki" DockPanel.Dock="Top" TabStripPlacement="Left" Margin="0,0,-689,-361">

                        </TabControl>
                        <Grid>
                            <Button Content="Перезагрузить" HorizontalAlignment="Center" Height="40" VerticalAlignment="Top" Click="Reload_zayav"  Width="92" Margin="0,395,0,0"></Button>
                            <Button Content="Добавить" HorizontalAlignment="Left" Height="40" VerticalAlignment="Top" Width="92" Click="ZayavAdd_click" Margin="5,395,0,0"/>
                        </Grid>
                    </Grid>

                </TabItem.Content>
            </TabItem>
            <TabItem Header="Оборудование">
                <TabItem.Content>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="60"></RowDefinition>
                            <RowDefinition Height="295"></RowDefinition>
                            <RowDefinition Height="0*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <DataGrid Background="#ffb56b" AutoGenerateColumns="False"  Grid.Row="1" x:Name="equipment" ItemsSource="{Binding Path=Equipment}" CanUserResizeRows="False" >
                            <DataGrid.Resources>
                                <Style TargetType="TextBlock" x:Key="WrapText">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Номер" Width="*" Binding="{Binding Path=id}" ElementStyle="{StaticResource WrapText}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Модель" Width="*" Binding="{Binding Path=model}" ElementStyle="{StaticResource WrapText}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Статус" Width="*" Binding="{Binding Path=status}" ElementStyle="{StaticResource WrapText}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Номер договора" Width="*" Binding="{Binding Path=client_id}" ElementStyle="{StaticResource WrapText}" ></DataGridTextColumn>
                               
                                <DataGridTemplateColumn Header="Действие" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="Equipment_edit_click">Подробнее</Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>

                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Canvas>
                            <Button Content="Перезагрузить" Canvas.Left="290" Canvas.Top="364" HorizontalAlignment="Left" Height="40" VerticalAlignment="Center" Click="Reload_equipment"  Width="92"></Button>
                            <Button Content="Добавить" Canvas.Left="22" Canvas.Top="364" HorizontalAlignment="Left" Height="40" VerticalAlignment="Center" Width="92" Click="EquipmentAdd_click"/>
                            <Button x:Name="otchet1" Content="Создать отчет" Canvas.Left="580" Canvas.Top="364" Height="40" Click="Equipment_excel"  Width="92" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <ComboBox Name="searchParam_equipment" IsEditable="False" IsReadOnly="True" Canvas.Left="241" Canvas.Top="19" HorizontalAlignment="Left" VerticalAlignment="Center" Width="141">
                                <ComboBoxItem Content="Номер"></ComboBoxItem>
                                <ComboBoxItem Content="Модель"></ComboBoxItem>
                                <ComboBoxItem Content="Статус"></ComboBoxItem>
                                <ComboBoxItem Content="Номер договора"></ComboBoxItem>
                            </ComboBox>
                            <TextBox x:Name="searchText_equipment" Canvas.Left="397" Canvas.Top="21" HorizontalAlignment="Left" VerticalAlignment="Center" Width="140"></TextBox>
                            <Button Content="Поиск" Click="findEquipment" Canvas.Left="554" Canvas.Top="20" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Width="65"></Button>
                        </Canvas>
                    </Grid>
                </TabItem.Content>
            </TabItem>
            
            <TabItem Header="Регистрация">
                <TabItem.Content>
                    <Grid>
                        <Frame x:Name="registration">
                            
                        </Frame>
                    </Grid>
                </TabItem.Content>
            </TabItem>

        </TabControl>
        <Canvas>
            <Button  Canvas.Bottom="40" Canvas.Left="0" Width="102" Height="40" Content="Выйти" Click="logOut" HorizontalAlignment="Center" VerticalAlignment="Bottom"></Button>
            <Button  Canvas.Bottom="0" Width="102" Height="40" Content="Закрыть" Click="Button_Click" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
        </Canvas>


        <Image Width="250" Height="150"
               Source="{Binding Source={StaticResource SampleData}, 
                                Converter={StaticResource ListToTextEncodingConverter},
                                ConverterParameter='http://chart.apis.google.com/chart?cht=lc&amp;chs=250x150&amp;chds=0,30'}" Margin="-449,171,999,129"/>
    </Grid>


</Page>
